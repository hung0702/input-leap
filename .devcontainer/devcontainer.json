{
  "name": "Input Leap Build Environment (Dockerfile)",

  // Instruct Codespaces to build the container from the Dockerfile
  // located in this same directory.
  "build": {
    "dockerfile": "Dockerfile"
  },

  // Specify the user to connect as, which we set up in the Dockerfile.
  "remoteUser": "vscode"
}
